/// @description Initialize
// Use declarations
uses props

// Define custom properties
a equals 28;
b equals "blablabla";
c equals "beeeeeeh";

// Show debug to confirm
log(prop[?a]);
log(prop[?b]);
log(prop[?c]);

log(no_props);